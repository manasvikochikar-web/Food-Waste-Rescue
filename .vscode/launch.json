{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flask: Food Waste Rescue",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/app.py",
            "console": "integratedTerminal"
        }
    ]
}
